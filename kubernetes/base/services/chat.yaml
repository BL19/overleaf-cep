apiVersion: v1
kind: Service
metadata:
  name: overleaf-chat
  namespace: overleaf
  labels:
    app.kubernetes.io/component: chat
spec:
  type: ClusterIP
  ports:
    - port: 3010
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: overleaf
    app.kubernetes.io/component: chat
