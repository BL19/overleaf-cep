apiVersion: v1
kind: Service
metadata:
  name: overleaf-contacts
  namespace: overleaf
  labels:
    app.kubernetes.io/component: contacts
spec:
  type: ClusterIP
  ports:
    - port: 3036
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: overleaf
    app.kubernetes.io/component: contacts
