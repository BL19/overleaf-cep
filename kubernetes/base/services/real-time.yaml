apiVersion: v1
kind: Service
metadata:
  name: overleaf-real-time
  namespace: overleaf
  labels:
    app.kubernetes.io/component: real-time
spec:
  type: ClusterIP
  ports:
    - port: 3026
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: overleaf
    app.kubernetes.io/component: real-time
