apiVersion: v1
kind: Service
metadata:
  name: overleaf-filestore
  namespace: overleaf
  labels:
    app.kubernetes.io/component: filestore
spec:
  type: ClusterIP
  ports:
    - port: 3009
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: overleaf
    app.kubernetes.io/component: filestore
