apiVersion: v1
kind: Service
metadata:
  name: overleaf-references
  namespace: overleaf
  labels:
    app.kubernetes.io/component: references
spec:
  type: ClusterIP
  ports:
    - port: 3056
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: overleaf
    app.kubernetes.io/component: references
